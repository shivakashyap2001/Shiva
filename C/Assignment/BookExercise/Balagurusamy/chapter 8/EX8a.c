#include<stdio.h>
#include<conio.h>
#include<string.h>
void main()
{
	int k,i,l;
	char dinar[100];
	printf("Enter a STRING:\n");
	gets(dinar);
	i=strlen(dinar);
	printf("the ASCII values of  the strinng are:\n");
	for(i=0;i<l;i++)
		{
		  k=dinar[i];
		  printf("%d",k);
		 }
}
